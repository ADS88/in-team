import { IconName } from "./icon-name"
//Interface for a student using the application
interface Student {
  firstName: string
  lastName: string
  id: string
  profileIcon: IconName
}

export default Student
